<div class="mt-2">
    <ul class="flex items-center gap-12">
      <% for (i=1;i<=pages;i++) {%>
        <li class="<%= page == i?'bg-red-600':'bg-red-200'%> text-white py-2 px-3 rounded-md cursor-pointer">
          <a href="?page=<%= i %>">
            <%= i %>
          </a>
        </li>
        <% }%>
    </ul>
  </div>